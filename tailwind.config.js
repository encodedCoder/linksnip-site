// tailwind.config.js (or .ts)
module.exports = {
  // ... your existing config
  theme: {
    extend: {
      // ... other extensions
      fontFamily: {
        mulish: ["var(--font-mulish)", "sans-serif"],
      },
    },
  },
  // ... rest of config
};